{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/generate-tasks.ts": { "maxDuration": 120 }
  },
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/" }]
}
